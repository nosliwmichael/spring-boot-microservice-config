server:
  port: ${discovery.service.port}

eureka:
  instance:
    instanceId: ${app.name}
  server:
    enableSelfPreservation: false
  client:
    registerWithEureka: false
    fetchRegistry: false

logging:
  level:
    com:
      netflix:
        eureka: OFF
        discovery: OFF